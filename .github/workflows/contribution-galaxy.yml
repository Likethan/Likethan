name: Generate Contribution City

on:
  push:
    paths:
      - "scripts/generate_contribution_galaxy.py"
      - ".github/workflows/contribution-galaxy.yml"
  schedule:
    - cron: "17 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install image dependency
        run: python -m pip install --disable-pip-version-check Pillow

      - name: Generate animated pixel city
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/generate_contribution_galaxy.py

      - name: Point README to animated pixel city
        run: |
          sed -i 's#contribution-galaxy\.gif#contribution-city.gif#g' README.md
          sed -i 's#contribution-galaxy\.svg#contribution-city.gif#g' README.md
          if git diff --quiet -- README.md; then
            echo "README already points to the pixel city"
          else
            git config user.name "Likethan"
            git config user.email "Likethan@users.noreply.github.com"
            git add README.md
            git commit -m "docs: use animated pixel contribution city"
            git push origin HEAD:main
          fi

      - name: Publish visualization
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: output
          folder: dist
          clean: false
